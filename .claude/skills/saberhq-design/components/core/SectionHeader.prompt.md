The signature section opener: 1.5px ink top rule, numbered uppercase mono label with the number in vermillion. Every major section on every surface starts with one.

```jsx
<SectionHeader number="01" label="Experience" />
<SectionHeader number="04" label="Selected Publications" aside={<a href="#">full list → Google Scholar</a>} />
```
