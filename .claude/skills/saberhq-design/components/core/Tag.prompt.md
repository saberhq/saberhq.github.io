Small mono chip for topics, stacks, and statuses (e.g. "Perturb-seq", "PyTorch").

```jsx
<Tag>Nextflow</Tag> <Tag accent>Virtual Cell Challenge</Tag>
```

Default is quiet (hairline + ink-3); `accent` for the one highlighted tag. Never more than one accent tag per group.
