# Blog UI kit

Article template for saberhq.com/blog in the Saber HQ brand, populated with real content from `content/docs/blog/autoencoder/autoencoder.md`. Static HTML on the token layer — the article body is typography, not components. Image slots are striped placeholders (real covers live in the repo under `static/images/art/`).
